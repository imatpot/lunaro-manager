version: "3"

services:
  lunaro-manager:
    image: "lunaro-manager"
    build: "."
    hostname: "lunaro-manager"
    container_name: "lunaro-manager"
    restart: "unless-stopped"
    volumes:
      - "./data:/mnt/app/data"
