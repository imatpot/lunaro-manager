version: "3"

services:
  lunaro-tracking-bot:
    image: "lunaro-tracking-bot"
    build: "."
    hostname: "lunaro-tracking-bot"
    container_name: "lunaro-tracking-bot"
    restart: "unless-stopped"
    command: "npm start"
    volumes:
      - "./data:/app/data"
    environment:
      DISCORD_TOKEN: "${DISCORD_TOKEN}"
      CLIENT_ID: "${CLIENT_ID}"
      TRACKED_GUILD_ID: "${TRACKED_GUILD_ID}"
      RTP_ROLE_ID: "${RTP_ROLE_ID}"
